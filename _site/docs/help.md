# help

## dumi 配置

.umirc.ts为doc，npx dumi dev不报错。改为site，提示： Cannot read property 'length' of undefined

YAML语法是不允许用tab来缩进，只能用空格。

设置date的时候，如果加上了具体时间（小时：分钟：秒），必须在后面写上+0800（时区偏移），否则将不会生成该文章页面

(入门网站)[https://www.jianshu.com/p/9f198d5779e6]

## Jekyll



